# NORA LoRA Training Requirements
# 用于训练和测试，不包含 LIBERO 仿真环境和数据 regeneration 依赖

# Core ML frameworks
torch>=2.4.0
torchvision>=0.19.0

# Transformers and model loading
transformers>=4.50.0
qwen_vl_utils
safetensors

# PEFT for LoRA
peft>=0.10.0

# Training utilities
accelerate>=1.5.2
wandb

# Flash attention (optional, for faster training)
#flash-attn>=2.6.1  # Uncomment if CUDA available and want faster attention

# Data processing
numpy>=1.26.4
scipy
pillow
h5py  # For HDF5 data loading

# Configuration
pyyaml
# omegaconf  # Not currently used, optional

# Optical flow (Phase C)
# torchvision already includes RAFT models

# Testing
pytest>=7.0.0
pytest-cov

# Utilities
tqdm
einops

# OpenCV for image processing (used by VQ encoder)
opencv-python

# FAST+ tokenizer (from physical-intelligence)
# 通过 HuggingFace 自动下载，无需单独安装
# 使用 AutoProcessor.from_pretrained("physical-intelligence/fast", trust_remote_code=True)
# 首次运行时会自动从 HuggingFace 下载代码和权重
